// write a program in c++ which will convert the RS to paise.
#include<iostream>
using namespace std;
int main()
{
    float n,p;
    cout<<"enter the cost/amount of the product";
    cin>>n;
    p=n*100;
    cout<<"The cost of the product in paise is"<<p;
    return 0;
}